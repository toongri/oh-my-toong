# oh-my-toong Global Configuration
# This file provides default settings for the sync system

# Global default platforms (sync.yaml can override at any level)
# Priority: component-level > section-level > sync.yaml top-level > use-platforms > hardcoded [claude]
use-platforms: [claude]

# Backup retention settings
backup_retention_days: 3
